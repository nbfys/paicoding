version: "3.8"
services:
  mysql:
    image: mysql:8.0
    container_name: paicoding-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    command: ["--character-set-server=utf8mb4","--collation-server=utf8mb4_unicode_ci"]

  redis:
    image: redis:6.2
    container_name: paicoding-redis
    restart: unless-stopped
    ports:
      - "6379:6379"