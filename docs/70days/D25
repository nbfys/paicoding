📝 D25 项目总结：数据持久化与异常兜底
1. 🎯 核心目的 (Purpose)
数据落地：将“阅后即焚”的 AI 对话过程持久化存储到 MySQL，为后续的数据分析（如 D26 的报表）提供数据源。
架构闭环：打通 Service -> Event -> Listener -> Mapper -> DB 的完整异步链路，验证架构的可行性。
鲁棒性建设：构建异常兜底机制，确保即使上游服务（AI 接口）不可用或超时，系统依然能记录故障现场，不丢单、不崩盘。
2. 🔨 实施过程 (Actions)
数据库层：
在 MySQL 中创建了 ai_chat_log 表。
使用 MyBatis-Plus 创建了对应的 Entity 和 Mapper。
业务层改造：
修改 AiEventListener，将原本的日志打印改为 aiChatLogMapper.insert(...)。
修改 AiService，引入 try-catch 块。
Happy Path: 调用成功 -> 发布包含 AI 回复的事件。
Error Path: 捕获异常 -> 发布包含错误信息的事件（兜底）。
配置调整：
遇到了 Spring AI 默认重试机制导致的超长等待问题，调整了 connect-timeout 和 read-timeout（虽因为配置文件格式问题波折了一下，但明确了排查方向）。
3. 💡 关键成果 (Result)
成功入库：
1+1=几 -> 1+1=2 (正常响应，耗时短，秒入库)。
失败兜底：
地球为什么是圆的 -> SocketTimeoutException (响应超时)。
现象：虽然前端早早收到了“任务已提交”，但在后台，Spring AI 默默重试了多次（日志显示 Retry count 1, 2, 10...），耗时几十秒后最终抛出异常。
结果：catch 块成功捕获异常，监听器忠实地将错误信息 【调用失败】...timeout 写入了数据库。
最终验证：刷新数据库，看到了 ID 1（成功）和 ID 2,3,4（失败）的所有记录。
4. 🧠 经验沉淀 (Key Takeaways)
重试机制的“副作用”：Spring AI 的自动重试虽然能提高成功率，但在超时场景下会极大地延长响应时间，甚至给人一种“程序卡死/没入库”的错觉。开发时需注意观察日志中的 Retry count。
异步的“子弹”：异步任务的执行时间可能远超预期（本例中长达 30秒+）。在排查“数据没写入”的问题时，要有耐心等待子线程跑完，或者检查是否因为父线程关闭导致子线程被杀。
配置文件的严谨性：application.properties 和 .yml 混用是大忌，会导致配置静默失效，排查成本极高。