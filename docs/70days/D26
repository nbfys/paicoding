📝 D26 项目总结：定时任务 (Scheduling)
1. 🎯 核心目的 (Purpose)
自动化运维：解放人力，让系统自动执行周期性任务（统计、清理、备份）。
数据变现：将沉淀在数据库里的冷数据（Logs）转化为有价值的热数据（报表/监控指标）。
轻量级调度：在不引入复杂的分布式调度平台（如 PowerJob/XXL-JOB）之前，掌握 Spring Boot 原生的轻量级解决方案。
2. 🔨 实施过程 (Actions)
启用调度：添加 @EnableScheduling 注解。
编写 Task：创建 AiScheduleTask 组件。
Cron 表达式：配置 0/10 * * * * ? 实现高频执行（10秒级），便于开发调试。
业务逻辑：结合 MyBatis-Plus 的 QueryWrapper，实现了“查总数”和“查近期”两种维度的统计。
3. 💡 关键成果 (Result)
任务存活：日志显示定时任务稳定运行，未出现中断或异常。
数据连通：定时任务成功读取了 D25 存入的 ai_chat_log 表数据。
集成验证：验证了 MyBatis-Plus 在非 Controller 环境下（即在定时任务线程中）依然能正常工作。
4. 🧠 经验沉淀 (Key Takeaways)
单线程陷阱：@Scheduled 默认是单线程的。如果你的 statDailyUsage 方法执行了 20 秒，那么下一次任务会被推迟，甚至阻塞其他定时任务。在生产环境，通常需要配置 TaskScheduler 线程池。
Cron 的魔力：掌握 Cron 表达式是后端开发的必备技能。
无状态设计：定时任务最好是无状态的，或者具备幂等性（重复执行不影响结果），防止因系统重启导致任务重复跑或漏跑。