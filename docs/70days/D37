📅 D37：全链路集成 (Service Integration)
🎯 核心目的
将 D36 (异步事件) 真正接入到 Paicoding 的业务流程中。
当用户发布/更新文章时，系统会自动触发 ArticlePublishEvent，后台悄悄调用 AI 生成摘要，且不影响前台响应速度。

🛠️ 怎么实现的

代码修改：在 ArticleWriteServiceImpl.saveArticle 方法中，注入了 ApplicationEventPublisher。
事件触发：在事务提交之后（确保文章已保存），判断 articleId 存在且内容不为空，然后发布异步事件。
全链路验证：
前端：发布文章操作流畅，无卡顿（日志显示接口耗时仅几十毫秒）。
后端：后台线程默默工作（虽然日志里没贴出来，但数据库结果说明了一切）。
数据库：article_ai_summary 表中成功新增了一条记录 (article_id=2605...)，摘要内容非常专业：“本文是一次针对Spring AI集成能力的功能验证实践...”。
✅ 最终结果

业务闭环：用户发布 -> 异步事件 -> AI 生成 -> 摘要入库 -> 前端展示（下次访问时）。
体验升级：原来的同步调用需要等待 3-5 秒，现在发布接口响应极快，AI 生成完全在后台完成。