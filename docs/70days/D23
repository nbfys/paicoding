📝 D23 项目总结：异步处理 (@Async)
1. 🎯 核心目的 (Purpose)
提升吞吐量：主线程（Tomcat 线程）不再傻等 AI 的龟速响应，可以立马释放去处理下一个用户的请求。
优化用户体验：用户点击按钮后瞬间得到反馈（"任务处理中"），而不是盯着白屏转圈圈。
削峰填谷：配合线程池的 Queue，即使瞬间涌入 1000 个请求，也会在队列里乖乖排队，不会把服务器内存撑爆。
2. 🔨 实施过程 (Actions)
开启异步：在启动类添加 @EnableAsync。
配置线程池：自定义 ThreadPoolTaskExecutor，设置核心线程数、最大线程数、拒绝策略等（生产环境必备）。
Service 下沉：将耗时操作封装到 AiService 的 @Async 方法中。
Controller 改造：改为“触发即返回”模式。
3. 💡 关键成果 (Result)
接口响应耗时从 3387ms (D22) 降到了 5ms (D23)。
控制台日志清晰地显示了主线程（nio-8081-exec-4）和子线程（task-1）的分离执行过程。
4. 🧠 经验沉淀 (Key Takeaways)
@Async 的自调用陷阱：同类调用（this.method()）不会触发 AOP，必须注入 Bean 调用。
线程池隔离：不要用 @Async 默认的线程池（SimpleAsyncTaskExecutor），它每来一个请求就 new 一个线程，极其危险。必须自定义线程池。
超时处理：异步任务一旦超时或报错，前端是收不到的（因为主线程早跑了）。因此异步任务必须配合日志记录或状态入库（D25 内容）。
